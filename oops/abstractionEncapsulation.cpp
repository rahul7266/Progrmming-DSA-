// encapsulation  menas encapsultaing data and function together in a single unit  forex- class
// abstraction means hiding irrilevant data from user 
// advantages of abstraction ---(1). updation and changes (internally kuch bhi change krne se end user affected nahi hoga)
                              //(2). Avoid the errors ( unauthorized user can't indtroduce error in the code)
                              