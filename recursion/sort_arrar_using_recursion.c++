#include<bits/stdc++.h>
using namespace std;
void s(vector<int>&v){
    if(v.size()==0)
    return;
    int x=v[v.size()-1];
    
}
int main(){
    int n;
    cin>>n;
    vector<int>v(n);
    for(int i=0;i<n;i++)cin>>v[i];
    s(v);
    return 0;
}