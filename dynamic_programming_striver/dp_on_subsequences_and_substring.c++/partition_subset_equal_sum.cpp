// do same as the question subset sum 
//only change is pass the target = total sum/2, because if sum/2 found in array then remaining will also be sum/2
//and if total sum is odd then it never give equal partition